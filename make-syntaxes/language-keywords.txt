# REXX LANGUAGE KEYWORDS

# simple keywords
DO
FOREVER
WHILE
UNTIL
TO
BY
FOR
END
EXIT
IF
THEN
ELSE
ITERATE
LEAVE
NOP
RETURN
SELECT
WHEN
OTHERWISE
OPTIONS
WITH
PULL
PUSH
QUEUE
SAY
ARG
DROP
INTERPRET
UPPER

# keywords with pattern
CALL(\\s+(OFF|ON)\\s+(ERROR|FAILURE(\\s+NAME)?|HALT))?
SIGNAL(\\s+(OFF|ON)\\s+(ERROR|FAILURE(\\s+NAME)?|HALT|NOVALUE|SYNTAX))
ADDRESS\\s+\\w+
NUMERIC\\s+(DIGITS|FORM(\\s+(SCIENTIFIC|ENGINEERING|VALUE))?|FUZZ)
PARSE(\\s+UPPER)?\\s+(EXTERNAL|NUMERIC|SOURCE|VALUE|VAR|VERSION)?|PROCEDURE(\\s+EXPOSE)?
TRACE\\s+\\w+
